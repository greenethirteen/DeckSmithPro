# Server-side ONLY. Never put keys in the browser.
OPENAI_API_KEY=sk-your-key-here
# Optional (defaults are chosen if omitted)
OPENAI_TEXT_MODEL=gpt-4o
OPENAI_IMAGE_MODEL=gpt-image-1

# Gemini (optional; only needed if you select Gemini in the UI)
GEMINI_API_KEY=your-gemini-key-here
# Defaults are fine, but you can override:
GEMINI_TEXT_MODEL=gemini-3-pro
GEMINI_IMAGE_MODEL=gemini-3-pro-image-preview

PORT=8787
# CORS for local dev frontend
CLIENT_ORIGIN=http://localhost:5173
